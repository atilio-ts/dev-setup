---
name: Use houtini without asking permission
description: Never ask the user for permission before using houtini MCP tools
type: feedback
---

Use houtini tools (mcp__houtini-lm__*) freely without asking the user for permission first.

**Why:** User explicitly said not to ask for permission — just use it.

**How to apply:** Any time houtini would be useful (delegating bounded tasks, offloading code generation, quick factual queries), invoke it directly without prompting the user.